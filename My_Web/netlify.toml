[build]
  command = "npm run build"
  publish = "dist"  # Update this to match the actual directory where Vite outputs the build artifacts

[build.environment]
  NODE_VERSION = "14"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
